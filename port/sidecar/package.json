{
  "name": "zettlr-sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Zettlr Node.js sidecar — temporary bridge during Electron→Tauri migration",
  "main": "main.ts",
  "scripts": {
    "start": "tsx --tsconfig tsconfig.json main.ts",
    "start:root": "tsx --tsconfig port/sidecar/tsconfig.json port/sidecar/main.ts",
    "build": "echo \"TODO: esbuild bundle for production\"",
    "test": "node --import tsx/esm --test test-provider.ts"
  },
  "dependencies": {
    "tsx": "^4",
    "tsconfig-paths": "^4"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22"
  }
}